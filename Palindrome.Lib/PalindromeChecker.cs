namespace Palindrome.Lib
{
    /// <summary>
    /// A palindrome is a word, phrase, or sequence that reads the same backward as forward.
    /// The PalindromeChecker class contains methods used to check various aspects of a palindrome.
    /// </summary>
    public class PalindromeChecker
    {
        /// <summary>
        /// Determines whether or not a given string is a palindrome.
        /// Spaces are not removed.
        /// The string being checked is not case sensitive.  I.e, "Radar" will return true.
        /// </summary>
        /// <param name="str"></param>
        /// <returns>true if the string is a palindrome.  False otherwise.</returns>
        public bool IsPalindrome(string str)
        {
            return false;
        }

    }
}